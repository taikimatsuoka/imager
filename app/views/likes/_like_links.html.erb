<% if current_user.likes.find_by(note_id: note.id) %>
    <%= link_to '同一E/U案件の紐付けを取り消す', unlike_path(note.id), method: :delete, remote: true %>
  <% else %>
    <%= link_to '同一E/U案件を紐付けする', like_path(note.id), method: :post, remote: true %>
<% end %>
